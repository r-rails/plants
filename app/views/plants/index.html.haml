-# list of all

.container
  %div.p-2 You can sort by name, latin or category. 

  %table.w-full.mt-4
    %tr.border-b.font-sans
      %th
        .flex.text-left.font-semibold=link_to 'name', :sort => 'name'
      %th
        .flex.text-left.font-semibold=link_to 'latin', :sort => 'latin'
      %th
        .flex.text-left.font-semibold ideallight
      %th
        .flex.text-left.font-semibold watering
      %th
        .flex.text-left.font-semibold climate
      %th
        .flex.text-left.font-semibold=link_to 'category', :sort => 'category'

    - @plants.each do |plant|
      %tr.bg-white.odd:bg-gray-100.hover:text-green-700
        %td.p-2.hover:underline= link_to plant.name, plant
        %td= plant.latin
        %td= plant.ideallight
        %td= plant.watering
        %td= plant.climate
        %td= plant.category
